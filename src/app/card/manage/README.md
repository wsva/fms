# card 数据
在 tag, card, card_tag 中新增字段
copy_from = old user_id
source_uuid = old uuid
复制数据时选择，是否保留复制标记
如果不保留复制标记，则无法清除
清除数据时只能清除带有复制标记的数据，可以指定清除复制字哪个用户的数据

查看某个用户的所有 card，按tag

clear 某个tag下的所有数据

clone 另一个用户的某个tag A下的所有数据到我的 tag B

export 导出自己m某tag下的所有数据

statistic 统计每个用户的数据量
按 tag 统计数据量